{
  "repository": {},
  "license": "UNLICENSED",
  "devDependencies": {
    "androiduix": "^0.6.7",
    "jsdom": "^7.2.2",
    "typescript": "^2.0.8"
  },
  "scripts": {
    "build": "npm run build-install && npm run build-res && npm run build-ts2js",
    "postbuild": "echo 'build finish!'",
    "build-install": "echo 'installing dependencies...' && npm install --registry=https://registry.npm.taobao.org",
    "build-res": "echo 'building resource...' && node build_res.js",
    "build-ts2js": "echo 'converting TypeScript to JS...' && \"node_modules/.bin/tsc\" -p ./",
    "updateSDK": "cd androidui-sdk && node update_sdk.js && cd .."
  }
}
